# Worker 的名称
name = "cfzy"
# 主入口文件
main = "src/index.mjs"
# 兼容性日期
compatibility_date = "2024-04-03"

# [vars]
# 您可以在这里为开发环境设置非加密变量
# 您的密钥 (Secrets) 必须使用 wrangler secret put 命令设置，而不是放在这里

[vars]
# 示例：这些值将从您设置的 Secret 中读取
# MS_TENANT_ID = "您的 TENANT_ID"
# MS_USER_ID = "您的 USER_ID"